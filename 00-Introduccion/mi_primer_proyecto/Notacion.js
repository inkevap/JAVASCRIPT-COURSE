// NOtacion
// ; <---- Delimitar el final de una linea
const b = 4;
b + 4;

// . <---- Se utiliza en los objetos para acceder a los atributos
console.log();

// [] <---- se usa para las listas arreglos o arrays
const ar = [1,2,3,4];
console.log(ar[2]);

// () <---- Se usa para los argumentos para las funciones
    function suma(a,b) {
    }
    


// {} <---- Se utilizan para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if(true){
    //todo lo que haya entre llaves
    const constante2 = "Hola"
}
