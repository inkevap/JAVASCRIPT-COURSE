let altura = 173;
let altura_mts = 1.73;
let peso_kg = 59.5;
let altura_mts_round = Math.ceil(altura_mts);
let peso_kg_round = Math.floor(peso_kg);
let valor_maximo = Number.MAX_VALUE + 1 === Number.MAX_VALUE;

console.log(altura);
console.log(altura_mts);
console.log(peso_kg);
console.log(altura_mts_round);
console.log(peso_kg_round);
console.log(valor_maximo);
